<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cloudwork.tenant.mapper.TenantMemberMapper">

    <insert id="insertTenantMember" parameterType="com.cloudwork.tenant.domain.TenantMember"
            useGeneratedKeys="true" keyProperty="memberId">
        insert into tenant_member (tenant_id, user_id, role_id, member_status, joined_at)
        values (#{tenantId}, #{userId}, #{roleId}, #{memberStatus}, #{joinedAt})
    </insert>

    <select id="selectAccess" resultType="com.cloudwork.tenant.vo.TenantAccessVo">
        select
            m.tenant_id as tenantId,
            m.user_id as userId,
            r.role_code as roleCode
        from tenant_member m
                 inner join tenant_role r on r.role_id = m.role_id and r.tenant_id = m.tenant_id
        where m.tenant_id = #{tenantId}
          and m.user_id = #{userId}
          and m.member_status = #{memberStatus}
          and r.status = 0
    </select>
</mapper>
